version: '3.8'

services:
  S4P_MNIST:
    build:
      context: .
      dockerfile: dockerfiles/Dockerfile
    container_name: S4P_MNIST_app
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8000:8000"
